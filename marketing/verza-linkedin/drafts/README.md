# Drafts (first pass from the generator)

Markdown here is **AI first pass** for you (Serge) to edit before LinkedIn.

- Filenames look like `2026-02-10-tue-playbooks.md`.
- Frontmatter includes `status: first_pass` until you change it to `ready` or `published`.
- You can commit drafts to git for an async review trail, or keep them local only.

**Do not** schedule posts directly from these files without your editor pass and a quick scan against `../BANNED_CLAIMS.md`.
